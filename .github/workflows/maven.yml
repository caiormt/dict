name: Build

on:
  # When new code is merged
  push:
    branches:
      - main
      - develop
    paths-ignore:
      - "**/README.md"
      - "**/CONTRIBUTING.md"
      - "**/CONTRIBUTORS.txt"
      - '**/CODE_OF_CONDUCT.md'
      - "**/LICENSE"
      - "**/CODEOWNERS"

  # When user pull request
  pull_request:
    branches:
      - main
      - develop
    types:
      - opened
      - synchronize
      - reopened

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: "corretto"
          java-version: "21"
          cache: "maven"

      - name: Check Style Guide
        run: ./mvnw -T 1C validate --file pom.xml

      - name: Compile with Maven
        run: ./mvnw -T 1C clean generate-sources compile --file pom.xml

      - name: Upload artifacts
        uses: actions/upload-artifact@v5
        with:
          name: build-output
          path: target/
          compression-level: 0
          if-no-files-found: error

  test:
    runs-on: ubuntu-latest
    needs:
        - build
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: "corretto"
          java-version: "21"
          cache: "maven"

      - name: Download artifacts
        uses: actions/download-artifact@v6
        with:
          name: build-output
          path: target/

      - name: Test with Maven
        run: ./mvnw -T 1C test-compile test --file pom.xml

  analyze:
   runs-on: ubuntu-latest
   needs:
       - test
   steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: "corretto"
          java-version: "21"
          cache: "maven"

      - name: Cache SonarQube packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Analyze with Maven
        run: ./mvnw -T 1C clean verify sonar:sonar -Dsonar.projectKey=caiormt_dict --file pom.xml
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
